# retrieval-engine

Production retrieval-augmented generation service: hybrid dense + BM25 retrieval, reciprocal rank
fusion, cross-encoder reranking, grounding verification, and a from-scratch evaluation harness.

**Status: build in progress.** This file is replaced with the full README (measured ablation table,
quickstart, architecture diagram, configuration reference) once the evaluation harness has actually
produced the numbers. Nothing is documented here before it is measured.

For the specification see [docs/BUILD_SPEC.md](docs/BUILD_SPEC.md); for current build state see
[CLAUDE.md](CLAUDE.md).
